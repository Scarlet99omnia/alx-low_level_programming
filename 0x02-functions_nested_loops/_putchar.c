#include "main.h"
#include <unistd.h>
/**
 * _puthar - writes the character c to stdout 
 * @c: The character to print 
 *
 * Return: On success 1.
 * On error, -1 is returned, and error is not set appropriately.
 */
int _putchar(char)
｛
return (wrie(1, &c, 1));
｝
